import React from 'react';


const BackgroundTheme = () => {
  return (
    <div>

{/* <body>
  <div className="relative z-10 max-w-screen min-h-screen bg-white overflow-hidden">
    {Array.from({ length: 50 }).map((_, i) => (
      <img
        src="./flower.png"
        key={i}
        className="absolute opacity-50 rounded-full"
        style={{
          width: "100px",
          height: "100px",
          top: `${Math.floor(Math.random() * 100)}%`,
          left: `${Math.floor(Math.random() * 100)}%`,
        }}
      />
    ))}

    <Header />
    <div className="mx-auto z-50 flex justify-center items-center container">
      {children}
    </div>
  </div>
  <Footer />
</body> */}

    </div>
  );
};

export default BackgroundTheme;
